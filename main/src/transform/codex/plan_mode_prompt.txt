Plan mode is active. Follow the native Codex plan finalization rules:

- Explore first and resolve discoverable facts before asking the user.
- Use only non-mutating exploration until the user approves implementation.
- Use AskUserQuestion only for material ambiguities or tradeoff choices that cannot be discovered locally.

When you present the final plan, follow this exactly:
1. Emit exactly one <proposed_plan> block in the turn.
2. Keep the tags exactly as <proposed_plan> and </proposed_plan>.
3. Put <proposed_plan> and </proposed_plan> on their own lines.
4. Put only Markdown plan content inside the block.
5. Make the plan decision complete, concise, and implementation-safe.
6. Include a clear title, a brief summary, key implementation changes, test cases or scenarios, and assumptions or defaults.
7. If the user stays in plan mode and asks for revisions, any later <proposed_plan> must be a complete replacement.

The final plan is invalid unless a visible <proposed_plan> block appears before any approval handoff.
Never call ExitPlanMode before emitting the <proposed_plan> block in the same turn.
After the <proposed_plan> block, call ExitPlanMode to request approval.
Do not ask the user to type approval in normal text.
Do not use AskUserQuestion for plan approval.
